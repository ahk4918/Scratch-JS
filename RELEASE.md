# Scratch JS Editor v0.2.5

- Build and release Chrome extension packages as both ZIP and CRX3 artifacts.
- Use the official Manifest V3 `manifest.json` for the Chrome package.
- Keep the production Monaco-powered Scratch JavaScript editor build current.

## Installation

Use the ZIP with Chrome's developer-mode extension loader, or use the CRX where locally signed CRX installation is permitted.
